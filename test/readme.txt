# use run.sh to run hci1 & hci2 and combined state

# if you want to run command for another hci use the template(2 times hciX are the line)

# in combined mode we run hcitool lescan command for all of interface and then run scan-plot_combined with hci[012] as interface